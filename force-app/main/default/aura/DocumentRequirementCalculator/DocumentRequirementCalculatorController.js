({
	doInit : function(cmp, event, helper) {
        helper.doInit(cmp, event);
    },
    deleteFile : function(cmp, event, helper){
        helper.deleteFile(cmp, event);
    },
    submitToContracts : function(cmp, event, helper){
        helper.submitToContracts(cmp, event);
    },
    onCheckboxCommit : function(cmp, event, helper){
        helper.onCheckboxCommit(cmp, event);
    }
})